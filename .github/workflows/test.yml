name: Unit Test
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello, Tyrone"
      - name: Print goodbye
        run: echo "Goodbye"
